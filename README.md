# Task Tracker CLI

A simple command-line task tracker built with Python. This project allows users to create, update, delete, list, and manage the status of tasks directly from the terminal.

This project was built as a hands-on exercise to practice Python fundamentals, command-line interfaces, file handling, JSON persistence, and basic software organization.

## Features

- Add new tasks
- Update existing tasks
- Delete tasks
- List all tasks
- Filter tasks by status
- Mark tasks as `in-progress`
- Mark tasks as `done`
- Store tasks locally using JSON

## Technologies

- Python 3
- JSON
- `sys`
- `datetime`
- `pathlib`

No external libraries or frameworks are required.

## Getting Started

### Prerequisites

Make sure Python 3 is installed on your machine.

Check your Python version:

```bash
python3 --version